generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model FavProduct {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model PurchaseList {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
